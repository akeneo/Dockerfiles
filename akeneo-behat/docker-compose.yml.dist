version: '2'

services:
  apache-php:
    build: ./
    depends_on:
      - mysql
      - mongodb
      - selenium
    volumes:
      - /path/to/you/pim:/home/docker/pim
    working_dir: /home/docker/pim
    ports:
      - '8080:80'
    networks:
      - behat

  selenium:
    image: selenium/standalone-firefox-debug:2.53.0
    ports:
      - '5900:5900'
    networks:
      - behat

  mysql:
    image: mysql:5.6
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_USER=akeneo_pim
      - MYSQL_PASSWORD=akeneo_pim
      - MYSQL_DATABASE=akeneo_pim
    networks:
      - behat

  mongodb:
    image: mongo:2.6
    networks:
      - behat

networks:
  behat: ~
